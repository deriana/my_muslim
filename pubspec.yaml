name: my_muslim
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_compass: ^0.8.1
  gap: ^3.0.1
  geodesy: ^0.10.2
  geolocator: ^13.0.2
  http: ^1.3.0
  sensors_plus: ^6.1.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
assets:
  - assets/
